.week-select-wrapper
    label(for="week-select") Select Week:
    select#week-select(name="select-week")
        - for (let w = 1; w <= 14; w++)
            option(
                value=w
                selected=w===currentWeek
                )= `Week ${w}`
