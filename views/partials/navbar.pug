nav.navbar
    .container
        a.brand(href="/") BBFB
        ul.nav-links
            li
                a.nav-link(href="/" class= page === 'homepage' ? 'current-page' : '') Home
            li
                a.nav-link(href="/matchups" class= page === 'matchups' ? 'current-page' : '') Matchups
        button.hamburger(type="button" aria-label="Toggle navigation")
            span
            span
            span
